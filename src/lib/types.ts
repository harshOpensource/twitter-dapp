export interface TrendingResult {
  heading: string;
  description: string;
  tags: string[];
  img: string;
}

export interface FollowResult {
  userImg: string;
  username: string;
  tag: string;
}
